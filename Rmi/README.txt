These are the most basic implementation of java RMI (Remote Method Invocation).
MyRmi is the interface implementd by the MyRmiImp and RmiClient that is accessing it.
The MyRmiImp and RmiClient should be run on diffent terminals.
rmiregistry should also be running for this to work on a different terminal.
